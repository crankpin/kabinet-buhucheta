# Текущий статус проекта

Версия: `(13) — content + SEO core`  
Дата: 5 августа 2026 года

Сборка: чистый `v12.1-wave-2a-clean` + интегрированная SEO-карта `docs/seo/seo-map-v1.md`.  
Код в `site/` и `legacy/` в версии **(13) как таковой** не менялся. Публичные страницы услуг по-прежнему stubs.

## Важно: несохранённая post-v13 правка в рабочей копии

После фиксации `(13)` в рабочей копии (без коммита) выполнена стабилизирующая правка главной страницы: ситуации, три честные группы сопровождения, единый монолит услуг, горизонтальная лента из 12 отзывов, фокус мобильного меню и контактной панели, деднение шрифтов и lazy-load декоративной анимации на mobile, а также точечные фиксы sitemap.xml, квиза, чек-листа и логотипа. Подробности и факт незакоммиченности — в `reports/` (см. последний post-v13 отчёт) и в выводе `git status`/`git log`. Этот раздел актуализировать или удалить после коммита.

## Что в этой сборке

- канон границ — `docs/service-matrix.md`;
- маркетинговое ядро — `docs/project-strategy/`;
- карта реализации 2A — `docs/reports/wave-2a-implementation-map.md`;
- SEO-ядро — `docs/seo/seo-map-v1.md` (+ доказательная таблица Wordstat);
- четыре коммерческих текста — `docs/content/pages/` с интегрированной SEO-оболочкой по карте.

## Статус текстов

- факты, цены, состав услуг, ограничения, ответственность и смысл обещаний — **канон**;
- SEO-оболочка четырёх страниц интегрирована по `seo-map-v1` (Title/Description/H1/точечные фразы/FAQ АУСН/темы консультации);
- дальнейшие точечные правки оболочки — только через обновление SEO-карты и утверждение владельцем.

## Обязательный порядок этапов

1. Волна 2A — **завершена**.
2. Семантическое ядро и SEO-карта — **завершены и интегрированы** в docs (шлюз пройден на уровне документации).
3. Версия **(13) — content + SEO core** — **текущая**.
4. До 2B закрыть решения по **CTA** и **hero**.
5. **Волна 2B** — эталонная страница ИП (код).
6. Отдельная проверка 2B.
7. **Волна 2C** — хаб, ООО на УСН, налоговое консультирование.
8. Перед публикацией — окончательная текстовая и техническая SEO-проверка.

| Этап | Статус |
|---|---|
| 2A | сделана |
| SEO-ядро + карта | сделаны, в `docs/seo/` |
| (13) content + SEO core | **текущая docs-сборка** |
| CTA / hero | блокеры до 2B |
| 2B / 2C | не начаты; публичные URL услуг ещё не реализованы |

## Что делать дальше

1. Закрыть CTA и hero.
2. Запускать 2B только отдельным заданием: одна страница ИП.
3. Не создавать PHP-страницы налогов/регионов/отраслей «заодно».
4. Не снимать `noindex` stubs до готовности контента в коде.

## Важное ограничение

Новые публичные страницы (налоги, регионы) в `(13)` **запланированы в SEO-карте**, но **не реализованы** на сайте. Меню и `site/` не менялись.
