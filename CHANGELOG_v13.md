# CHANGELOG v13 — content + SEO core

Дата: 5 августа 2026 года  
Тип: только документация; `site/` и `legacy/` не изменялись

## Сделано

- добавлен пакет `docs/seo/`: `seo-map-v1.md`, `wordstat-semantic-core-2026-08-04.xlsx`, копия промпта v13;
- в четырёх `docs/content/pages/*.md` интегрирована SEO-оболочка из раздела 3 карты (Title/Description/H1, лид хаба, режимы ИП, FAQ про АУСН, темы консультации);
- статусы страниц: `редакционно утверждено; SEO-оболочка интегрирована по seo-map-v1`; версии документов увеличены на минорный шаг;
- в `docs/sitemap.md` география сведена к трём URL: Москва+МО, Ростов+область, ДНР+ЛНР; отдельные области/ДНР/ЛНР и региональные копии услуг запрещены явно;
- `README_FIRST.md` переведён на версию `(13) — content + SEO core`; SEO-шлюз отмечен пройденным на уровне docs; CTA/hero и 2B сохранены как следующие блокеры.

## Не делалось

- правки PHP/CSS/JS/JSON и меню;
- снятие `noindex`;
- создание страниц налогов и регионов на сайте;
- решения CTA/hero;
- старт волны 2B.

## Обновление (актуально на момент написания, без нового номера версии)

Начиная с записи выше в `site/` внесена отдельная **post-v13 стабилизирующая правка** (пока не закоммичена). Она не относится к контенту/SEO этого changelog и подробно описана в отдельном post-v13-отчёте в `reports/`. Строка «`site/` и `legacy/` не изменялись» выше верна только для самой волны `(13)` и больше не описывает текущее состояние рабочей копии — см. `git status`.
